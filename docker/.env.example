# ============================================================
# 🔒 Archimedes V2 — Variáveis de Ambiente (docker/.env)
# ============================================================
# ⚠️ NUNCA versione este arquivo real. Copie este template:
#
#   cp .env.example .env
#
# Em seguida gere uma chave secreta forte e cole no .env:
#
#   openssl rand -hex 32
#
# ============================================================

# Chave secreta do Open-WebUI (obrigatória)
WEBUI_SECRET_KEY=coloque-aqui-uma-chave-de-32-bytes-hex